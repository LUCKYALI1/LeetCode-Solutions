long long count = 0 , lastOcc = 0;

for(auto num : nums){
    if(num == 0){
        lastOcc++;          // extend zero streak
        count += lastOcc;   // add new subarrays ending here
    }
    else{
        lastOcc = 0;        // reset streak when non-zero
    }
}
